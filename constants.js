const dbConnection = {
	user: '-----------',
	password: '-----------',
	host: '-----------',
	database: '-----------',
	waitForConnections: true,
	connectionLimit: 10,
	queueLimit: 0
}

module.exports = {
	dbConnection
}
